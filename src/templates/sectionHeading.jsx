const SectionHeading = ({ text }) => (
    <h2 className="text-3xl md:text-4xl font-bold text-brand-700 dark:text-brand-400 text-center">
        {text}
    </h2>
);

export default SectionHeading;
