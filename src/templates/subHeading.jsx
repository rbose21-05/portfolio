const SubHeading = ({ text }) => (
    <p className="text-lg md:text-xl text-slate-600 dark:text-slate-400 text-center max-w-3xl mx-auto leading-relaxed">
        {text}
    </p>
);

export default SubHeading;
